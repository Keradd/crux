# Task completions

Document finished tasks here. Filename pattern:
`YYYY-MM-DD-task-name.md`

These files are gated by `.claudeignore`. They cost 0 tokens until an agent
is asked to load one specifically.
